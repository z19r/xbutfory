Welcome, @<%= @user.handle %>

One click and your XbutforY account is confirmed (the link is good for two days):

<%= confirm_email_url(token: @token) %>

If you didn't create this account, ignore this email.

— The XbutforY editors
