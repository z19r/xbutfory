Hi @<%= @user.handle %>,

Someone asked to reset the password for your XbutforY account. If that was you,
pick a new one within 15 minutes:

<%= edit_password_reset_url(token: @token) %>

If you didn't request this, ignore this email — your password stays as it is.

— The XbutforY editors
