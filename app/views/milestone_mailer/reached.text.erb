Hey <%= @user.display_name.presence || "@#{@user.handle}" %>,

<%= @entry.title %> just crossed <%= @milestone %> votes. The readers have spoken;
the formula holds.

See where it stands: <%= entry_url(slug: @entry.slug) %>

— The XbutforY editors

---
You get these because milestone notifications are on. Turn them off any time in your
settings: <%= account_settings_url %>
