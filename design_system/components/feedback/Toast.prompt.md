The house toast — deadpan microcopy on an ink chip with a hard offset shadow. Use it for the directory's running commentary (vote milestones, easter eggs, "RSS: point your reader at /feed.xml").

```jsx
<div style={{ position: 'fixed', bottom: 24, right: 24 }}>
  <Toast>🔮 Y-values scrambled. Find the hidden genius in the chaos.</Toast>
</div>
```

- Keep copy short, lowercase-ish, and dry. One emoji at the start is on-brand; more than one is not.
- Positioning + auto-dismiss are the caller's job; the component is purely the surface.
